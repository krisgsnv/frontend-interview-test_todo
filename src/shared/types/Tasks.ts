export interface TasksState {
    id: string;
    name: string;
    description: string;
    category: string;
  }

  export interface AddTask {
    name: string;
    description: string;
    category: string;
  }